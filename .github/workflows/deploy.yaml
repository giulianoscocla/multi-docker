name: Deploy MultiDocker
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t giulianoscocla/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true giulianoscocla/react-test npm test

      - run: docker build -t giulianoscocla/multi-client ./client
      - run: docker build -t giulianoscocla/multi-nginx ./nginx
      - run: docker build -t giulianoscocla/multi-server ./server
      - run: docker build -t giulianoscocla/multi-worker ./worker

      - run: docker push giulianoscocla/multi-client
      - run: docker push giulianoscocla/multi-nginx
      - run: docker push giulianoscocla/multi-server
      - run: docker push giulianoscocla/multi-worker